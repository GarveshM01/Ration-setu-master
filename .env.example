# Optional Supabase persistence for WhatsApp demo activity.
# Use the publishable/anon key only. Never put a service-role key in the frontend.
# Base project URL only (do not append /rest/v1; the repository adds it).
VITE_SUPABASE_URL=https://occnwieivslpjagdhemi.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional read-only backend proxy for MySQL-style beneficiary records.
# Never point the browser directly at MySQL; the endpoint should expose
# GET /beneficiaries and return the same shape documented in README.md.
VITE_BENEFICIARY_API_URL=
